# index settings
caseSensitiveIndexBasename: ${caseSensitiveIndexBasename}
stemmedIndexBasename: ${stemmedIndexBasename}

# Build/Run settings
journal: ${journal}
year: ${year}
fulltext.root: ${fulltext.root}
dataset.a.path: ${dataset.a.path}
medline-root: ${medline-root}